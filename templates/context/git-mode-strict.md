- The user set git mode to **strict** in totopo - git commands that modify the repository and remote access (push, pull, fetch, clone) are blocked and return a clear error. Read-only inspection commands (e.g. `git status`, `git log`, `git show`, `git diff`, `git blame`, `git branch --list`, `git rev-parse`) work - use them freely.
