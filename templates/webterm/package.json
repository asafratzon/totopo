{
    "name": "webterm",
    "version": "0.1.0",
    "private": true,
    "description": "Browser front-end for the interactive agent CLIs (claude, opencode, codex) inside a totopo container (PTY relay + rich composer).",
    "type": "module",
    "scripts": {
        "start": "node server.js"
    },
    "dependencies": {
        "@xterm/addon-fit": "^0.10.0",
        "@xterm/xterm": "^5.5.0",
        "express": "^4.21.2",
        "node-pty": "^1.0.0",
        "ws": "^8.18.0"
    }
}
